<main class="article">
{{#unless page.attributes.no-toolbar}}{{> toolbar}}{{/unless}}
  
  <div class="content">
    {{> article}}
    {{#if (ne page.attributes.layout 'page-wide')}}
    {{> toc}}
    {{/if}}
  </div>
  
</main>
