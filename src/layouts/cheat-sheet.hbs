<!DOCTYPE html>
<html lang="en"{{#unless page.attributes.no-dark-mode}} style="color-scheme: dark;"{{/unless}}>
  <head>
{{> head defaultPageTitle='Untitled'}}
  </head>
  <body class="light-mode dark-mode article{{#if page.attributes.banner}} has-banner{{/if}}{{#with (or page.attributes.role page.role)}} {{{this}}}{{/with}}{{#with asciidoc.attributes.role}} {{{this}}}{{/with}} docs cheat-sheet{{#with (or page.attributes.styles page.styles)}} {{{this}}}{{/with}}{{#if (or page.attributes.show-labels page.show-labels)}} show-labels{{/if}}"
    data-page-url="{{page.url}}"
    data-page-component="{{page.component.name}}"
    data-page-version="{{page.version}}"
    data-page-tabs="{{lookup page.attributes 'tabs'}}"
    data-page-tabs-group="{{lookup page.attributes 'tabs-group'}}"
    data-no-local-manifest="{{lookup page.attributes 'no-local-manifest'}}"
    data-page-toc-levels="{{{or page.attributes.toclevels 2}}}"
    data-ui-root-path="{{{uiRootPath}}}"
    data-site-path="{{site-path site.url}}">
<script type="application/json" id="page-nav-data">{{{json-stringify-script page.navigation}}}</script>
{{> header}}
{{> body}}
{{> footer}}
  </body>
</html>
